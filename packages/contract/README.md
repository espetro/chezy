# packages/contract

Wire types / domain contracts shared between `apps/web` and `apps/scraper`. Valibot
schemas on the TS side; Pydantic models on the Python side (mirrored manually until
a codegen pipeline lands).

Day-0 stub. The first schemas land when the first cross-package shape is needed.
