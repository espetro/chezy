// Public surface of @chezy/config.
// Day-0 stub. The Valibot env parser lands when the first env-dependent
// feature ships. Direct process.env reads are banned everywhere else by
// .oxlintrc.json; this package is the single source of truth.
export const PLACEHOLDER = "valibot env parser lands in the first feature ticket";
