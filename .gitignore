# Dependencies
node_modules/
.pnpm-store/

# Python
.venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
.mypy_cache/
.basedpyright_cache/

# pg0 embedded Postgres data
.pg0/

# Local data stores
*.db
*.db-shm
*.db-wal
*.db-journal
local.db

# Scraper output (raw payloads, JSONL datasets, audit logs)
.data/
*.jsonl
!apps/scraper/tests/fixtures/**/*.jsonl
.audit/

# Secrets / env
.env
.env.local
.env.*.local
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp
*.swo
*~

# Test / coverage
coverage/
.nyc_output/
playwright-report/
test-results/
.playwright-cli/

# Build artifacts
.turbo/
.next/
.tanstack/
*.tsbuildinfo
.output/

# Logs
*.log
.logs/
.audit/
pyinfra-debug.log

# Plans are intentionally tracked even though a global gitignore may silence them
!.agents/plans/

# Per-developer scratch (secrets, dumps)
tmp/
scratch/

# betterleaks report — written by the workflow; never track locally
betterleaks-report.sarif

# Vercel CLI link state — never tracked
.vercel/

# Next.js
next-env.d.ts

# Dataset photos are 437 MB of WebP; the UI hotlinks media[].url at the source
# CDN instead. Keep the tables (data/, schema/, docs) tracked.
chezy-mock-data/media/

# apps/video generated media: TTS WAVs, teammate phone captures, renders
apps/video/public/audio/
apps/video/public/clips/
apps/video/out/
apps/evals/.galtea-ids.json
